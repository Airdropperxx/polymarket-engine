# Secrets — NEVER commit these
.env

# SQLite database (binary, tracked separately; WAL/SHM are temp files — NEVER commit)
data/trades.db-wal
data/trades.db-shm
data/market_cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
*.pyc

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# HuggingFace model cache (large, not needed in repo)
# NOTE: GHA workflow caches ~/.cache/huggingface/hub separately
.cache/
